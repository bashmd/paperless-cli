[build-system]
requires = ["maturin>=1.6,<2.0"]
build-backend = "maturin"

[project]
name = "pcli-rust-norm"
version = "0.1.0"
description = "Optional Rust whitespace normalizer for pcli."
requires-python = ">=3.12,<3.15"
